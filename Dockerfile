# Используйте официальный образ Node 20
FROM node:20-alpine

# Создайте и установите рабочую директорию
WORKDIR /usr/src/app

# Скопируйте файл package.json и package-lock.json в рабочую директорию
COPY package*.json ./

# Установите зависимости
RUN npm install

# Скопируйте все файлы приложения в рабочую директорию
COPY . .

# Соберите приложение
RUN npm run build

# Укажите порт, который ваше приложение использует
EXPOSE 3000

# Команда для запуска вашего приложения
CMD ["npm", "start"]